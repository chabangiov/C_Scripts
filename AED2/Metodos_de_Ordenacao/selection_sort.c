/*
 *  Selection Sort
 *  (For each step it select the higher/lower element to replace the current array position)
 */
#include <stdio.h>

void troca (x, y);

void selection_sort (int *v, int N) {
    for (int i = 0; i < (N-1); i++) { // Tell the current position

    }
}

void troca (x, y) {
    int aux;
    aux = x;
    x = y;
    y = aux;
}