/*
 *  Insertion Sort
 *  (Similar to a cards game hand: every time you take a card, you already place it at correct position)
 *  
 */
#include <stdio.h>

void lower_card (int *v, int N);

void insertion_sort (int *v, int N) {

    int hand[N];
    for (int i = 0; i < (N-1); i++){

    }
}

void lower_card (int *v, int N) {
    int card;
    for (int i = 0; i < (N-1); i++){
        if (v[i] < v[i+1]);
    }
}
